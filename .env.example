# Copiar a .env en la raíz del repo (opcional). Valores por defecto en docker-compose.yml
DENTURA_DB_HOST=localhost
DENTURA_DB_PORT=5432
DENTURA_DB_NAME=dentura
DENTURA_DB_USER=dentura
DENTURA_DB_PASSWORD=dentura

# Solo para desarrollo local. Nunca activar en instalación de cliente.
# Con true habilita POST /api/demo/seed (super_admin). No inserta data al arrancar.
# DENTURA_DEMO_SEED_ENABLED=true
